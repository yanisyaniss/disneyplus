specVersion: "v1beta"
serviceId: "codespaces-react"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "codespaces-react-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
